@page
@model TvedcAdmin.Pages.OfficeExpert.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AboutUs_OfficeExpert[0].AssistantId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AboutUs_OfficeExpert[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AboutUs_OfficeExpert[0].Role)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AboutUs_OfficeExpert[0].Contact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AboutUs_OfficeExpert) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AssistantId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contact)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                <a href="~/Contacts/index?Id=@item.Id">تماس</a>
            </td>
        </tr>
}
    </tbody>
</table>
